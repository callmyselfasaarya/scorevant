{
  "name": "scorevant",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend && npx shx rm -rf public && npx shx cp -r Frontend/dist public",
    "preview": "npm run preview --workspace=frontend"
  },
  "workspaces": [
    "Frontend"
  ],
  "dependencies": {
    "@supabase/ssr": "^0.10.3",
    "@supabase/supabase-js": "^2.105.4"
  }
}
